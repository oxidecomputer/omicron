[package]
name = "sp-sim"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"

[dependencies]
anyhow = "1.0"
gateway-messages = { path = "../gateway-messages" }

#[dependencies.slog]
#version = "2.7"
#features = [ "max_level_trace", "release_max_level_debug" ]

[dependencies.tokio]
version = "1.16"
features = [ "full" ]

[[bin]]
name = "sp-sim"
